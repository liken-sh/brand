# The coverage profile the gate measures. `make test` writes it on
# every run and reads it once; it never belongs to the tree.
/coverage.out

# The Rust build tree. cargo writes it under iced/ and rebuilds
# anything it needs.
/iced/target/
