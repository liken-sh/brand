## The document itself

The API serves the document this page is made from at
`/v1/widget/openapi.json`, and this site publishes the same copy at
[/docs/reference/openapi.json](/docs/reference/openapi.json).
